// components/Header.jsx
import styles from "./Header.module.css"; 

const Header = () => (
  <header className={styles.header}>
    <h1>Book Now</h1>
    <h2>Once-in-a-lifetime experience that stays with you for a lifetime.</h2>
    <h3>Journey you don't want to miss</h3>
  </header>
);

export default Header;