# Example Dockerfile - You will build on a base image
FROM node:18-alpine 

# Install Python and pip (Required for Selenium)
RUN apk add --no-cache python3 py3-pip 

# Set working directory
WORKDIR /app

# Copy package files and install Node dependencies
COPY package*.json ./
RUN npm install

# Copy application code
COPY . .

# Install Python dependencies (Selenium)
RUN pip install selenium msedgedriver # msedgedriver might need special installation

# You can define the entrypoint to run your tests here, or run them from Jenkins
# ENTRYPOINT ["/bin/sh"]